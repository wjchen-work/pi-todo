# AGENTS.md — pi-todo 插件开发协作规范

本文件用于指导 AI 编码助手（Agent）在参与本项目开发时的行为准则。所有在本仓库内工作的 Agent **必须**遵守以下规则。

---

## 核心原则：不得猜测用户意图

> **遇到任何模糊、不明确、需要主观判断的决策点时，必须先向用户确认，再继续执行。**
>
> **绝不允许凭借"可能的意图"自行做出选择并直接落地代码。**

这一原则高于一切其他便利性考量。即使 Agent 认为某个选择"显然"更好，只要用户的意图无法从已有信息中确定性推断出来，就必须停下并确认。

---

## 一、什么情况下必须向用户确认

以下场景**必须**触发确认流程（使用 `ask` 工具提供选项）：

### 1. 需求层面的歧义
- 用户的描述存在多种合理解释（例如："优化一下"——优化什么？性能？可读性？API？）
- 多个并列需求之间的优先级未明确（例如：先做 A 还是先做 B？）
- 范围边界不清（例如："支持 Markdown"——支持哪些 Markdown 语法？是否需要支持扩展？）

### 2. 技术选型与架构决策
- 需要在多个等价方案之间选择（例如：状态管理用类还是函数？存储用文件还是 SQLite？）
- 引入新的依赖、第三方库、构建工具
- 改变现有架构、目录结构、模块边界
- API 设计的命名、参数顺序、返回值形态有多种合理选项

### 3. 用户体验与交互决策
- 快捷键、命令名、UI 文案的措辞
- 错误信息的提示风格
- 默认行为与可选行为的边界（例如：默认是否覆盖已有文件？默认是否自动保存？）

### 4. 破坏性或不可逆操作
- 删除文件、目录、依赖
- 改动已发布 API、已合并的代码结构
- 修改 `package.json`、`tsconfig.json`、`eslint.config.js` 等配置文件的语义性变更
- 执行 `git push`、`npm publish` 等对外操作

### 5. 与项目既有规范冲突
- 现有代码风格与新需求有冲突，且没有明确规范可参考
- 改动会影响其他扩展、命令或公共工具的行为

---

## 二、什么情况下**不必**确认（直接执行）

为避免过度打扰，以下情况 Agent 可以直接执行而无需确认：

- **语法/拼写/格式化修复**：如修正错别字、统一缩进、补全分号
- **明确的 lint / typecheck 报错**：错误信息已经给出唯一修复路径
- **用户刚刚明确指示的操作**：例如"把这个函数改名为 foo"，意图清晰
- **常规重构**：变量重命名、提取函数、拆分大文件——只要不改变对外行为
- **文档注释更新**：与代码改动严格对应的注释、README 修改
- **既有约定延续**：项目里已经有明确惯例（如命名规范、目录结构），按惯例行事

**判断口诀：意图能从"已有事实"中确定性推导出 → 直接执行；只能从"用户偏好"中推断 → 确认。**

---

## 三、如何向用户确认

### 3.1 使用 `ask` 工具
项目提供 `ask` 工具用于结构化提问。每次提问：
- 最多 **3 个问题**
- 每个问题 **2–3 个选项**
- 第一个选项为推荐项（Agent 认为最合理的默认）
- 选项末尾允许附加"自行输入"（free-form）作为兜底

### 3.2 提问内容的写法
- 问题应聚焦在**决策点本身**，不要重复背景
- 推荐选项需附带简短说明（`description`），让用户知道为什么 Agent 倾向该选项
- 避免在一个问题里塞入多个子决策——必要时拆成多个问题

### 3.3 一次问完，不要挤牙膏
如果一个任务存在多个独立决策点（例如：选库 + 命名 + 默认行为），应**一次性**用 `ask` 的多问题能力问完，不要每改一处问一次。

---

## 四、确认后的执行纪律

- 用户在 `ask` 中给出的回答**就是最终意图**，必须严格按回答执行
- 如果回答过程中用户补充了新的限制条件（"但不要改 X"），需要把它们一并纳入实现
- 不允许"先按自己理解写一版，再问要不要改"——这是变相猜测
- 如果执行中发现回答无法落地（例如选型与现有约束冲突），**重新回到确认流程**，而不是自行妥协

---

## 五、使用 `tree` 快速了解项目结构

在开始任何改动之前，Agent 应先用 `tree` 命令（**必须排除 `node_modules`**）建立项目的整体认知，避免遗漏相关文件或误判改动影响范围。

### 推荐命令

```bash
# 仅查看顶层目录结构（进入新上下文时首选）
tree -L 2 -I 'node_modules'

# 查看完整目录树
tree -I 'node_modules'

# 多模式过滤：排除 node_modules、构建产物、版本控制目录
tree -I 'node_modules|dist|build|.git'

# 兜底方案：环境无 tree 时使用 find
find . -not -path '*/node_modules/*' -not -path './node_modules' | head -50
```

### 何时使用

- 首次接触本项目，或长时间未编辑、需要重新进入上下文时
- 用户提到"那个文件""相关模块"但未给出具体路径时
- 准备做跨文件改动、重构或新增功能前，盘点现有结构以避免盲改

### 注意事项

- **始终排除 `node_modules`**：本项目依赖体积大，输出噪音会淹没真正的项目结构
- 仅看顶层用 `-L 2` 或 `-L 3`，深递归时再放开层级
- 精简环境若没有 `tree`，**不要省略这一步**，改用 `find` 兜底
- 输出结果可与 `package.json`、`tsconfig.json` 等配置文件交叉对照，再决定改动策略

---

## 六、项目专属约束（pi-todo）

本项目是 pi 编码代理的 todo 插件，使用 TypeScript + ESLint flat config。在确认决策时，请同时考虑以下既有约束：

- **扩展入口**：`src/index.ts` 是 pi 扩展的唯一注册文件，新增功能通常在此注册命令/快捷键/UI 组件
- **类型检查**：`pnpm check` = `tsc --noEmit && eslint .`，提交前必须通过
- **不要新增依赖**：除非用户明确同意，不向 `package.json` 添加新依赖（包括 `@types/*`、`eslint-plugin-*` 等）
- **不要改动构建/质量配置**：`tsconfig.json`、`eslint.config.js` 的语义性变更需特别确认
- **保持扩展轻量**：本插件是 todo 工具，能力范围外的大改动（如新增 HTTP 服务、数据库引擎）需要明确询问

---

## 七、遇到不确定时的默认动作

当 Agent 拿不准"该不该问"时，**默认选择"问"**。

判断标准很简单：

| 情况 | 动作 |
|---|---|
| 用户已经给出明确、唯一的指令 | 直接执行 |
| 存在歧义、偏好、未知约束 | **使用 `ask` 确认** |
| 不确定是否属于歧义 | 倾向于确认（提问成本 < 返工成本） |

---

## 八、违反原则的处理

若 Agent 在对话中已经被指出违反了"不猜测用户意图"的原则（例如用户回复"我没让你这么做"），应当：

1. 立即停止当前改动
2. 回退到上一个稳定状态（必要时用 `git diff` 评估影响）
3. 重新发起确认流程，明确列出"我准备这样做 X，是否同意"
4. 在得到明确同意前不重新落地

---

**记住：多问一句是礼貌，猜错一次是返工。**